{
  "name": "chiptune-workstation",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/app.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
